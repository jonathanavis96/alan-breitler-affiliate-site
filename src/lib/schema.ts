/**
 * Schema markup utility file.
 *
 * Pages construct their JSON-LD schema objects inline in their frontmatter
 * rather than using shared generator functions. This file is kept as a
 * placeholder in case shared schema utilities are needed in M2.
 */
